# Resolvia Database

Off-chain relational data layer.

Used for application metadata, indexing, search, analytics and protected information that should not be stored directly on-chain.

Planned database: PostgreSQL.
