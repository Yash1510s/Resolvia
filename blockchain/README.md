# Resolvia Blockchain

Smart-contract layer for trust-critical arbitration state.

Planned modules include CaseRegistry, EvidenceRegistry, JuryManager, VotingManager, VerdictManager, ReputationManager, Stake/Token and access/emergency controls.

Planned stack: Solidity + Hardhat + OpenZeppelin.
